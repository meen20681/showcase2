package com.example.admin.gRPC;

import io.grpc.StatusRuntimeException;
public class RolePermissionGrpcService {
/*
    public RolePermissionGrpcService(RolePermissionServiceGrpc.RolePermissionServiceBlockingStub blockingStub) {
        this.blockingStub = blockingStub;
    }

    public boolean doesRoleIdExist(long roleId) {
        try {
            RoleIDResponse response = blockingStub.checkRoleIdExistence(RoleIDRequest.newBuilder().setRoleId(roleId).build());
            return response.getExists();
        } catch (StatusRuntimeException e) {
            // Handle exceptions or log errors here
            return false; // Return false as a default value
        }
    }

 */
}
